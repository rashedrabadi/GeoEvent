<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.geoevent">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".deleteEvent"></activity>
        <activity android:name=".CheckIn" />
        <activity android:name=".reset" />
        <activity android:name=".TeamRegister" />
        <activity android:name=".StudentLogin" />
        <activity android:name=".studentDashboard" />
        <activity android:name=".addevent" />
        <activity android:name=".teamdashboard" />
        <activity android:name=".Register" />
        <activity android:name=".TeamLogin" />
        <activity android:name=".StudentLoginRegister" />
        <activity android:name=".TeamStudent" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>